package com.gildedrose;

public class Conjured extends ItemCategory {
    @Override
    void updateQualityOf(Item item) {
        super.updateQualityOf(item);
        super.updateQualityOf(item);
    }
}
